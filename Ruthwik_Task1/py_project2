class target_Sum:
    arr = [10,20,10,40,50,60,70]
    target = 50
    dict = {}
    sum = 0;
    for i in range(0,len(arr)):
        for j in range(0,len(arr)):
            if((arr[i]+arr[j]) == target):
                sum+=1
                dict.update({sum:[i,j]})

    print(dict)
